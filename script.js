var video = document.getElementById("myVideo");

var btn = document.getElementById("myBtn");

document.addEventListener("DOMContentLoaded", () => {
  const modal = new bootstrap.Modal(document.getElementById("syracuseModal"));
  modal.show();
});
